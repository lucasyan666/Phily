import 'package:flutter/material.dart';

/// Phily's brand accent — the warm gold used across the camera chrome, gallery,
/// and loaders. Single source of truth; don't re-declare the literal elsewhere.
const Color kGold = Color(0xFFE5C158);
